using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ingredientSpawner : MonoBehaviour
{
    public GameObject ingredient;

    //uhh how do you grab a copy of something rather than uh yknow the original when you grab the original
    //maybe just put it in the same spot?
    //& spawn another object at the spawner's position


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
